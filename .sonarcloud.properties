# Read by SonarCloud Automatic Analysis (sonar-project.properties is only used
# by explicit scanner runs). The pinned ts-stack conformance vectors are public
# synthetic test data; their auth headers and AES-GCM tags trip the hard-coded
# secret rule (S6418). Refresh them with scripts/sync-conformance-vectors.sh.
sonar.exclusions=internal/conformance/testdata/**
